pipeline:
    build:
        image: haavardlian/cmake:latest
        commands:
            - mkdir build
            - cd build
            - export CXX=${COMPILER}
            - cmake .. -DCMAKE_BUILD_TYPE=${BUILT_TYPE} 
            - make

matrix:
    COMPILER:
        - g++
        - clang
    BUILD_TYPE:
        - Debug
        - Release
